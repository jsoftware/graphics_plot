NB. test0

require 'numeric trig'

NB. dat=. sin steps 0 8 50
NB. pd 'reset'
NB. pd 'showboxes 1'
NB. pd 'backcolor ANTIQUEWHITE'
NB. pd 'type line'
NB. pd 'pensize 2'
NB. NB. pd 'fontscale 0.75'
NB. NB. pd 'fontscale 2'
NB. pd 'guidesize 1'
NB. pd 'title sin x^2'
NB. pd dat
NB. pd dat + 0.5
NB. pd 'type stick'
NB. pd dat

pd 'reset'

pd 'textcolor brown'
pd 'textfont arial 40 bold underline angle900'
pd 'textr 500 500 asdadZZZ'

pd 'title moo'
pd 'titlecolor red'
pd 'titlefont arial 60 bold underline'
pd 1 2 4 7
pd 'eps'
